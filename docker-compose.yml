services:
  db:
    image: postgres:18-alpine
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"
      - "POSTGRES_USER=sunjinlee"
      - "POSTGRES_DB=dart"
    volumes:
      - db-volume:/var/lib/postgresql
    ports:
      - "5433:5432"
    networks:
      - open-trading-network

  redis:
    image: redis/redis-stack-server:7.4.0-v8
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - open-trading-network

  dart-filing-worker:
    build:
      context: .
      dockerfile: Dockerfile_dart_filing_worker
    env_file:
      - .env.docker
    networks:
      - open-trading-network
    depends_on:
      - db
      - redis

  dart-filing-api:
    build:
      context: .
      dockerfile: Dockerfile_dart_filing_api
    ports:
      - "8080:8080"
    env_file:
      - .env.docker
    networks:
      - open-trading-network
    depends_on:
      - db
      - redis

  market-collector:
    build:
      context: .
      dockerfile: Dockerfile_market_collector
    env_file:
      - .env.docker
    networks:
      - open-trading-network
    depends_on:
      - db

networks:
  open-trading-network:
    name: open-trading-network
    driver: bridge

volumes:
  db-volume:
  redis-data:
